const initialState = []

export default function destinationsReducer(state = initialState, action) {
   switch (action.type) {
      case '':
         break
      default:
         return state
   }
}